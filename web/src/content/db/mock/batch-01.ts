import type { Question } from "@/lib/types";

/**
 * Задания из пробников по базам данных. Партия 01.
 *
 * Партия пуста намеренно. Исходный блок партии 01 целиком взят из файла
 * «БД и АиСД.docx», и на этот отрезок пришлась только половина «АиСД»:
 * 22 задания подряд по C++ (функция main, unsigned int, директива #undef,
 * оператор «запятая», инициализация массива), по сортировкам и поиску
 * (сортировка вставками, линейный поиск, индекс разбиения в быстрой сортировке),
 * по оценкам сложности в «О-синтаксисе», по сбалансированным деревьям,
 * хеш-таблицам с разрешением коллизий цепочками и по ориентированным графам.
 *
 * Ни одно из них не относится к базам данных и не ложится ни на одну тему
 * спецификации (data-models, relational-model, relational-algebra-ops, er-model,
 * functional-dependencies, aggregate-functions, filtering, sorting-grouping,
 * joins, ddl, dml, subqueries, normalization, denormalization), поэтому все
 * 28 блоков партии пропущены. Место этих заданий — в банке по алгоритмам.
 *
 * Собственно задания по базам данных начинаются со следующей партии:
 * источник «бд пробный 1 нуска.docx», первый вопрос — «Команда добавления
 * данных в таблицу».
 */
export const batch01: Question[] = [];

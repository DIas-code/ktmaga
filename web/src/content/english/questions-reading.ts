import type { Question } from "@/lib/types";

/**
 * ФАЙЛ СГЕНЕРИРОВАН массовым импортом. Не редактировать вручную.
 *
 * Задания по дисциплине «Английский язык — блок чтения», восстановленные из банка пробного
 * тестирования с готовыми ключами ответов (источник не установлен — задания удалены).
 *
 * Особенности этого набора, о которых стоит знать:
 *   - вариантов ответа ЧЕТЫРЕ, тогда как на самом КТ по этой дисциплине их пять;
 *   - уровень трудности (A/B/C) в источнике не указан и проставлен эвристикой
 *     по формулировке задания, а не взят из официальной спецификации;
 *   - разбор краток: источник содержит только ключ, без объяснений.
 *     Развёрнутая теория — в конспектах соответствующих тем.
 */
export const englishReadingQuestions: Question[] = [];

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /**
   * Сайт открывают с телефона по локальной сети, а `next dev` по умолчанию
   * пускает к своим dev-ресурсам (`/_next/*`, HMR) только тот хост, на котором
   * запущен, — то есть localhost. Без этого списка переходы по ссылкам с других
   * устройств молча не работают: страница открывается, а клики ничего не делают,
   * потому что роутер не может дозагрузить чанки.
   *
   * Локальные адреса меняются — поэтому разрешены обе частные подсети целиком.
   */
  allowedDevOrigins: ["192.168.*.*", "10.*.*.*"],
};

export default nextConfig;
